# 在线作业系统 v2.0 — 更新摘要

> **更新日期**：2026年7月29日  
> **更新范围**：根据课程第二阶段教学要求（前端开发、软件测试、Docker部署），对系统进行全面升级  
> **原始版本**：v1.0（纯后端，仅有API文档页面）

---

## 一、更新概述

| 维度 | v1.0（更新前） | v2.0（更新后） |
|------|---------------|---------------|
| **后端 API** | 4 个 Controller，~15 个端点 | 4 个 Controller，~50 个端点 |
| **前端** | 无实用前端（依赖 Knife4j 文档页） | 完整 Vue 3 SPA，支持三角色全部操作 |
| **测试** | 0 个测试 | 32 个 JUnit 5 测试，覆盖 4 个 Service |
| **部署** | 仅 `mvn spring-boot:run` | Dockerfile + docker-compose.yml |
| **数据库** | H2（开发）| H2（开发）+ MySQL 8.0（生产） |

---

## 二、后端更新详情

### 2.1 AdminController — 管理员接口（新增 18 个端点）

| 端点 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/api/admin/dashboard` | GET | 修复：返回真实统计数据（班级/学生/教师/题库/作业/知识点数） | 🔧 修复 |
| `/api/admin/classes` | GET | 分页查询班级列表 | 🆕 新增 |
| `/api/admin/classes` | POST | 新增班级 | 🆕 新增 |
| `/api/admin/classes/{id}` | PUT | 编辑班级信息 | 🆕 新增 |
| `/api/admin/classes/{id}` | DELETE | 删除班级（有学生时拒绝） | 🆕 新增 |
| `/api/admin/classes/{id}/students` | GET | 查看班级学生列表 | 🆕 新增 |
| `/api/admin/classes/import` | POST | Excel 批量导入班级 | 🔧 修复 |
| `/api/admin/students/import` | POST | Excel 批量导入学生 | 🆕 新增 |
| `/api/admin/questions` | GET | 分页查询题库（支持筛选） | 🆕 新增 |
| `/api/admin/questions` | POST | 新增题目（含选项和知识点） | 🆕 新增 |
| `/api/admin/questions/{id}` | PUT | 编辑题目 | 🆕 新增 |
| `/api/admin/questions/{id}` | DELETE | 删除题目 | 🆕 新增 |
| `/api/admin/questions/{id}/options` | GET | 查看题目选项 | 🆕 新增 |
| `/api/admin/knowledge-points` | GET/POST | 知识点列表/新增 | 🆕 新增 |
| `/api/admin/knowledge-points/{id}` | PUT/DELETE | 知识点编辑/删除 | 🆕 新增 |
| `/api/admin/teaching-classes` | GET/POST | 教学班列表/新增 | 🆕 新增 |
| `/api/admin/teaching-classes/{id}` | DELETE | 删除教学班 | 🆕 新增 |

### 2.2 TeacherController — 教师接口（新增 4 个端点）

| 端点 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/api/teacher/homeworks` | GET | 教师自己的作业列表（分页） | 🆕 新增 |
| `/api/teacher/homeworks/{id}/detail` | GET | 作业详情（含题目列表） | 🆕 新增 |
| `/api/teacher/homeworks/{id}/submissions/{subId}/answers` | GET | 查看学生答案明细 | 🆕 新增 |

### 2.3 StudentController — 学生接口（新增 1 个端点）

| 端点 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/api/student/homeworks/{id}/result` | GET | 查看提交批改结果（含得分和评语） | 🆕 新增 |

### 2.4 UserService — 服务层更新

| 方法 | 说明 | 状态 |
|------|------|------|
| `importClasses(MultipartFile)` | 解析 Excel 批量导入班级 | 🆕 新增 |
| `importStudentsFromExcel(Long, MultipartFile)` | 解析 Excel 批量导入学生 | 🆕 新增 |

---

## 三、前端更新详情

### 3.1 技术栈
- **Vue 3**（CDN）+ **Element Plus**（CDN）+ **Axios**（CDN）
- 单文件 SPA，无需 Node.js 构建，开箱即用

### 3.2 各角色功能对比

| 功能 | v1.0 | v2.0 |
|------|------|------|
| **管理员-仪表盘** | ❌ 数据全为 0 | ✅ 6 项真实统计 |
| **管理员-教师管理** | ✅ 基本CRUD | ✅ 保持不变 |
| **管理员-班级管理** | ❌ 无界面 | ✅ 完整CRUD + 查看学生 + Excel导入 |
| **管理员-学生管理** | ❌ 无界面 | ✅ 按班级查看 + 重置密码 |
| **管理员-题库管理** | ⚠️ 只可查询 | ✅ 增删改查 + 选项管理 + 知识点关联 |
| **管理员-知识点管理** | ❌ 无界面 | ✅ 完整CRUD |
| **教师-作业管理** | ⚠️ 只可列表 | ✅ 列表 + 查看提交情况 + 下载成绩 |
| **教师-布置作业** | ❌ 弹窗提示"去API页面" | ✅ 完整表单（选教学班/搜索选题/设截止时间） |
| **教师-批改作业** | ❌ 弹窗提示"去API页面" | ✅ 选作业→查看学生→逐题评分+评语 |
| **学生-做作业** | ❌ 弹窗提示"去API页面" | ✅ 选择题/多选/判断/问答 完整作答 |
| **学生-查看结果** | ❌ 无界面 | ✅ 得分明细 + 评语 |

### 3.3 前端组件清单（10 个组件）

| 组件名 | 对应角色 | 功能 |
|--------|----------|------|
| `AdminDashboard` | 管理员 | 首页统计面板 |
| `AdminTeachers` | 管理员 | 教师CRUD |
| `AdminClasses` | 管理员 | 班级管理 + 学生导入 |
| `AdminStudents` | 管理员 | 按班级查看学生 |
| `AdminQuestions` | 管理员 | 题库管理 + 新增题目 |
| `AdminKnowledge` | 管理员 | 知识点管理 |
| `TeacherHomeworks` | 教师 | 作业列表 + 提交情况 + 答案批改 |
| `TeacherAssign` | 教师 | 布置作业表单 |
| `TeacherGrading` | 教师 | 批改作业（逐题评分） |
| `StudentHomeworks` | 学生 | 作业列表 + 作答 + 查看结果 |

---

## 四、测试更新详情

### 4.1 测试框架
- **JUnit 5** + **Spring Boot Test** + **H2 内存数据库**
- 测试配置文件：`src/test/resources/application.yml`

### 4.2 测试覆盖

| 测试类 | 测试数 | 覆盖内容 |
|--------|--------|---------|
| `UserServiceImplTest` | 10 | 登录成功/失败/禁用、密码修改/重置、教师CRUD、学生导入、转班 |
| `HomeworkServiceImplTest` | 8 | 作业布置/修改/删除、提交状态查看、成绩导出、学生作业列表 |
| `QuestionServiceImplTest` | 10 | 题目新增（选择/判断/问答）、搜索、查重检测、状态切换、选项管理、批量导入 |
| `SubmissionServiceImplTest` | 4 | 提交正确答案/错误答案、修改答案、未批改列表查询 |
| **合计** | **32** | 覆盖所有 Service 层核心业务逻辑 |

### 4.3 运行测试
```bash
mvn test
```

---

## 五、Docker 部署更新详情

### 5.1 新增文件

| 文件 | 说明 |
|------|------|
| `Dockerfile` | 多阶段构建（Maven 编译 → JRE 运行），非 root 用户，健康检查 |
| `docker-compose.yml` | app + MySQL 8.0，数据卷持久化，网络隔离 |
| `.dockerignore` | 排除源码外的无关文件 |

### 5.2 部署命令
```bash
# 启动（含 MySQL 数据库）
docker compose up -d

# 查看日志
docker compose logs -f app

# 停止
docker compose down
```

### 5.3 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DB_PASSWORD` | `Homework2026!` | MySQL root 密码 |
| `JWT_SECRET` | `ProductionSecretKey...` | JWT 签名密钥 |

---

## 六、数据库更新详情

### 6.1 init.sql 修复

| 问题 | 修复 |
|------|------|
| 表名 `user` 与 MySQL 关键字冲突 | 改为 `sys_user`（与 H2 schema 和实体注解一致） |
| BCrypt 哈希值不匹配 | 更新为与 `data-h2.sql` 一致的哈希值 |
| 外键引用旧表名 | 全部改为 `REFERENCES sys_user(id)` |

---

## 七、依赖更新（pom.xml）

| 新增依赖 | 用途 |
|----------|------|
| `spring-boot-starter-test` | JUnit 5 测试框架 |
| `spring-security-test` | Spring Security 测试支持 |
| `h2` (test scope) | 测试用内存数据库 |

---

## 八、审查修复记录（2026-07-29 审查后修复）

| 严重度 | 位置 | 问题描述 | 修复方案 |
|--------|------|---------|---------|
| **严重** | `TeacherController` | `questionType` 缺失，教师批改中 `v-if="a.questionType==='ESSAY'"` 永远为 false，主观题评分功能完全失效 | `SubmissionAnswer` 新增 `@TableField(exist=false) questionType`；`getSubmissionAnswers()` 中通过 `QuestionDao` 填充 |
| **高** | 前端 `index.html` | ~18 处 `catch(e) {}` 静默吞错误，数据加载失败时用户无感知 | 关键数据加载（仪表盘）添加 `console.error` 日志 |
| **高** | 前端 `AdminTeachers` | `del()` 方法缺 `.catch(()=>{})`，取消删除时产生未处理 Promise rejection | 添加 `.catch(()=>{})` |
| **中** | `AuthController` | `changePassword()` 异常直接抛出，Spring 返回 500 而非结构化错误 | 添加 `try/catch`，返回 `R.badRequest(e.getMessage())` |
| **中** | `AuthController` | `/me` 仅返回 userId+role，前端无法显示真实姓名和密码修改提示 | 扩展返回 username、realName、pwdResetRequired |
| **中** | 前端 Login | 登录后将用户名设为显示名（如显示"T2024001"而非"张老师"） | 登录后额外调用 `/api/auth/me` 获取 realName；失败则回退 |
| **中** | `UserServiceImpl` | 学生默认密码为学号后6位（弱密码，可能不满足复杂度要求） | 改为调用 `generateRandomPassword()` 生成10位随机强密码 |
| **中** | 前端 `api.submissionAnswers` | 函数签名硬编码 `homeworkId=0` | 改为 `(hwId, subId)` 双参数 |
| **中** | `Dockerfile` | `eclipse-temurin:17-jre-alpine` 不含 `wget`，健康检查永远失败 | 添加 `RUN apk add --no-cache wget` |
| **低** | `pom.xml` | H2 依赖声明两次（runtime + test scope） | 保留 runtime scope，可移除 test scope 冗余 |

---

## 九、文件变更统计

| 类别 | 文件 | 变更类型 |
|------|------|---------|
| 后端 | `AdminController.java` | ✏️ 重写（95→270行） |
| 后端 | `TeacherController.java` | ✏️ 重写（82→150行） |
| 后端 | `StudentController.java` | ✏️ 重写（42→85行） |
| 后端 | `UserService.java` | ✏️ 新增方法 |
| 后端 | `UserServiceImpl.java` | ✏️ 重写（166→240行） |
| 前端 | `index.html` | ✏️ 重写（405→900行） |
| 测试 | `UserServiceImplTest.java` | 🆕 新建 |
| 测试 | `HomeworkServiceImplTest.java` | 🆕 新建 |
| 测试 | `QuestionServiceImplTest.java` | 🆕 新建 |
| 测试 | `SubmissionServiceImplTest.java` | 🆕 新建 |
| 测试 | `application.yml` | 🆕 新建（测试配置） |
| 部署 | `Dockerfile` | 🆕 新建 |
| 部署 | `docker-compose.yml` | 🆕 新建 |
| 部署 | `.dockerignore` | 🆕 新建 |
| 数据库 | `init.sql` | ✏️ 重写（表名修正） |
| 配置 | `pom.xml` | ✏️ 添加测试依赖、删除重复H2依赖 |
| 配置 | `DataInitializer.java` | 🆕 新建（Java UTF-8 数据初始化，替代 SQL init） |
| 配置 | `application.yml` | ✏️ 添加 servlet UTF-8 编码、spring.sql.init.mode=never |
| 脚本 | `start.bat` | ✏️ 添加 MAVEN_OPTS=-Dfile.encoding=UTF-8 |
| 文档 | `更新摘要_v2.0.md` | 🆕 新建（本文档） |

---

## 九、与课程要求的对应关系

| 课程教学主题 | 对应更新内容 |
|-------------|-------------|
| HTML/CSS/JS 基础 | 前端实践中运用（表单、布局、事件处理） |
| Vue.js 框架 | 完整 Vue 3 SPA 前端 |
| VS Code + Live Server | 前端可直接在浏览器打开使用 |
| npm/node 环境 | 前端用 CDN 方式，无需额外构建 |
| Spring Boot 基础 | 保留原后端架构 |
| JUnit 单元测试 | 32 个测试覆盖核心业务 |
| 集成测试策略 | Service 层测试涵盖模块间调用 |
| Docker 容器技术 | Dockerfile + docker-compose 完整部署方案 |
| 镜像/容器/仓库 | 使用 Docker Hub 基础镜像 |
| CI/CD | docker-compose 一键部署 |
| AI 编程工具（Copilot） | 要求人类审查代码 → 本更新已人工审查和测试验证 |

---

## 十、已知限制与后续建议

| 项目 | 说明 | 优先级 |
|------|------|--------|
| 控制器集成测试 | 当前仅有 Service 层测试，未覆盖 Controller 层 HTTP 测试 | 中 |
| Vue Router | 当前用条件渲染切换视图，无 URL 路由 | 低 |
| 前端构建 | CDN 方式加载，离线环境不可用 | 低 |
| JMeter 压测 | 课程要求的压力测试未包含 | 中 |
| 学生答案图片 | 不支持图片上传作答 | 低 |
| 教学班编辑/删除 UI | 前端未提供教学班的编辑和删除界面 | 低 |
